/*
 * September 2014
 */
package order;
import java.util.Scanner;
public class Order {
    public static void main(String[] args) {
        int x,y,a,b;
        int z = 0;
        a = 0;
        b = 0;
        System.out.println("This program will order two numbers in ascending order (smallest to largest)");
        System.out.println("Enter the first number");
        Scanner in = new Scanner (System.in);
        x = in.nextInt();
        System.out.println("Ender the second number");
        y = in.nextInt();
        if(x < y)
        {
            a = x;
            b = y;
        }
            else if(x > y)
            {
            b = x;
            a = y;
            }
        System.out.println("" + a + ", " + b);
    }
}
